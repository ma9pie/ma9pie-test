{
  "compilerOptions": {
    // JavaScript 코드의 ECMAScript 버전 설정
    "target": "es5",
    // TypeScript 컴파일러가 사용할 라이브러리를 설정
    "lib": ["dom", "dom.iterable", "esnext"],
    // 외부 라이브러리의 선언 파일을 검사하지 않도록 설정(컴파일 속도 관련)
    "skipLibCheck": true,
    // CommonJS와 ES 모듈 사이의 상호 운용성을 위한 코드 생성
    "esModuleInterop": true,
    // CommonJS 모듈을 ES6 default import로 가져올 수 있도록 허용
    "allowSyntheticDefaultImports": true,
    // TypeScript의 엄격한 모드 활성화 (타입 검사 강화 및 안전성 향상)
    "strict": true,
    // 파일 이름의 대소문자 일관성을 강제로 설정
    "forceConsistentCasingInFileNames": true,
    // JavaScript 모듈 생성
    "module": "esnext",
    // Node.js의 모듈 해석 알고리즘 사용
    "moduleResolution": "node",
    // JSON 파일을 모듈처럼 가져올 수 있도록 설정
    "resolveJsonModule": true,
    // JSX 코드의 컴파일 방식 설정
    "jsx": "react",
    // TypeScript .d.ts 타입 선언 파일 생성
    "declaration": true,
    // 컴파일된 JavaScript 파일들이 저장될 디렉토리 설정
    "outDir": "dist",
    // 모듈을 해석할 때 상대 경로의 기준이 되는 기본 경로 설정
    "baseUrl": ".",
    // 모듈 경로 별칭 설정
    "paths": {
      "@/*": ["src/*"]
    }
  },
  // 컴파일할 파일 지정
  "include": ["src"],
  // 컴파일에서 제외할 파일 지정
  "exclude": ["**/*.stories.ts", "**/*.test.ts"]
}
